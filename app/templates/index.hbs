<main class='container'>
  <div class='p-5 rounded mt-5'>
    <Header />
    {{#if this.model}}
      <GifWidget @model={{this.model}} />
    {{else}}
      <div class='bounce-loader'>
        <div class='bounce1'></div>
        <div class='bounce2'></div>
        <div class='bounce3'></div>
      </div>
    {{/if}}
  </div>
</main>